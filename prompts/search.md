# 后端搜索阶段说明

本阶段不调用模型。后端根据以下来源选择查询：

1. 第一轮使用 Plan 阶段给出的首个 `initial_queries`。
2. Judge 判断证据不足后，第二轮使用其首个 `needed_next_queries`。
3. 每轮只调用一次 Tavily Search。
4. Tavily 返回的完整响应和网页正文单独持久化；传给 Judge 的内容只保留最多 6 个高相关 Source Card 和限长正文片段。
5. 搜索执行、超时、重试、结果数量、Credit 用量和正文读取情况由后端审计日志记录。

模型不直接执行搜索，也不能构造或补编搜索结果。
